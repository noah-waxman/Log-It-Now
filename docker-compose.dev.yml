services:
  backend:
    build:
      context: ./backend
    ports:
      - "${BACKEND_PORT}:8000" # host:container
    command: npm run dev # hot reload
    volumes:
      - ./backend:/app
      - /app/node_modules
